[pytest]
addopts = --tb=short -v
testpaths = tests
markers =
    smoke:      Pruebas de humo — flujos críticos del happy path (login exitoso)
    critical:   Pruebas críticas — errores principales y validaciones de seguridad
    regression: Pruebas de regresión — comportamientos secundarios y edge cases
